내장 객체를 세 가지로 분류해보자
-브라우저 객체 모델
-문서 객체 모델
-전역 자바스크립트 객체

브라우저 객체 모델은 브라우저 탭 혹은 창의 모델을 생성한다.

최상위 객체는 window 객체로, 현재 브라우저 창이나 탭을 표현하는 객체이다.
이 객체의 자식 객체들은 브라우저의 다른 기능등을 표현 

Window 현재 브라우저 창이나 탭
  ---- Document 현재 로드된 웹 페이지
  ---- History 브라우저 히스토리에 기록된 웹 페이지들
  ---- Location 현재 페이지의 URL
  ---- Navigator 브라우저와 관련된 정보
  ---- Screen 장치의 디스플레이 정보 
  
  문서객체 모델 
  문서 객체 모델(DOM: Doucment Object Model)은 현재 웹 페이지의 모델을 생성한다.
  최상위 객체로는 document 객체이며, 전체 페이지를 표현한다. 
  자식 객체로는 페이지의 다른 요소들을 표현하는 객체들이 사용됨 
  
  document 
  ex)
  <html>
  <head>
  <title></title>
  </head>
  <body>
  <div>
  </div>
  <p></p>
  </body>
  </html>
  
  전역 자바스크립트 객체 
  전역 객체는 어떤 특정 모델을 표현하지 x
  전역 객체의 이름은 String이나 Date와 같이 주로 대문자로 시작함 
  다음의 객체들은 기본적인 데이터 타입을 표현한다.
  String  문자열 값들을 다룬다.
  Number 숫자 값들을 다룬다.
  Boolean 불리언 값들을 다룬다.
  
  다음의 객체들은 실제 세계의 개념들을 다루기 위한 것들이다.
  Date 날짜를 표현하며 조작한다.
  Math 숫자 값을 다루며 이들을 이용한 다양한 연산을 수행한다.
  RegEx 텍스트를 구성하는 문자열 내의 패턴처리를 담당한다.
  
  ex)
  String 객체의 toUpperCase() 메서드는 변수의 모든 문자들을 대문자로 변환한다.
  hotel.toUpperCase();
  
